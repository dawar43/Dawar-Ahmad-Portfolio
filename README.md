# Dawar Ahmad Portfolio Sandbox

This workspace is now set up with a simple local sandbox for previewing the static portfolio site.

## Run locally

- From the workspace root, run: `npm start`
- Or use the VS Code task: `Start Agent Sandbox`
- Open: http://127.0.0.1:8000/
